/** @type {import('next').NextConfig} */
const config = {
  // reactStrictMode: true,
  output: "export",
  basePath: "/archive",
  compiler: {
    // removeConsole: true,
  },
}

module.exports = config
